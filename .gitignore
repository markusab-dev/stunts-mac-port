# Stunts Native Port - Git Ignore

# Original proprietary assets (never commit copyrighted game data)
original/
Original/
extracted/
reference/restunts/stunts/
*.zip
*.ZIP

# Reference repositories (managed as submodules or external checkouts)
reference/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Build outputs
build/
bin/
obj/
dist/
*.o
*.obj
*.a
*.lib
*.dylib
*.so
*.exe
*.com
*.app/
*.dSYM/

# Test outputs & state dumps
*.bin
*.bni
*.BIN
*.BNI
dump/
test_output/

# IDE / Editor configuration
.vscode/
.idea/
*.swp
*~
*.idb
*.i64

# Generated test output. All of this is reproducible by running the tools -
# state dumps, rendered frames and visual captures - and it is 110 MB.
# tests/replays/ and tests/dos_reference/ are NOT ignored: the replays are
# inputs verify.sh needs, and the DOS captures are ground truth that cannot
# be regenerated without a DOSBox run.
tests/visual_captures/
tests/native_states/
tests/reference_states/
tests/opt_states/
tests/render_native/
tests/render_diff/
tests/render_oracle/
tests/render_reference/
*.bmp
*.jsonl

# Python bytecode
__pycache__/
*.pyc
